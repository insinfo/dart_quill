enum MaxHeightRatio {
  half,
  oneThird,
  quarter
}

enum NumberType {
  arabic,
  chinese
}

enum ImageDisplay {
  inline,
  block,
  surround,
  floatTop,
  floatBottom
}

enum LocationPosition {
  before,
  after,
  outerBefore,
  outerAfter
}

enum FlexDirection {
  row,
  column
}