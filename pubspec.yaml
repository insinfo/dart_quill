name: dart_quill
description: A web app that uses AngularDart
version: 1.0.0
publish_to: none
environment:
  sdk: ^3.6.0

dependencies:
  
  # quill_delta:
  #   git:
  #     url: https://github.com/pulyaevskiy/quill-delta-dart.git
  #     ref: master
  #ngdart: ^8.0.0-dev.4
  #ngforms: any
  #ngrouter: any

  #collection: ^1.17.0
  #diff_match_patch: ^0.4.1
  #quiver: ^3.2.1
  #collection: ^1.18.0
  #equatable: ^2.0.7
  #meta: ^1.15.0
  web: ^1.1.1
  # Única dependência além de `web`, e só o import de HTML externo a
  # alcança (dart_quill_html.dart): parsear HTML de fora é área de segurança
  # e de casos de borda, onde uma implementação caseira erra mais do que
  # economiza. Decidido em 2026-07-30, ver doc/PLANO_PORT_CONVERSORES_HTML_PDF.md.
  html: ^0.15.4

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  test: ^1.24.0

  ## para ui testes no navegador
  puppeteer: ^3.19.0
  shelf: ^1.4.2
  shelf_static: ^1.1.3
  webdev: ^3.0.0
