name: dart_quill
description: >-
  A pure-Dart port of the Quill rich text editor and the quill-table-better
  plugin for Dart web applications, with DOCX/PDF conversion, syntax
  highlighting and LaTeX formulas built in.
version: 1.0.0
publish_to: none
environment:
  sdk: ^3.6.0

dependencies: 
  web: ^1.1.1
  html: ^0.15.4

dev_dependencies:  
  test: ^1.24.0

  ## para ui testes no navegador
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  puppeteer: ^3.19.0
  shelf: ^1.4.2
  shelf_static: ^1.1.3
  webdev: ^3.0.0
